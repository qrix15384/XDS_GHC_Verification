# Build results
bin/
obj/

# User-specific files
*.user
*.suo

# Rider/VS
.vs/
.idea/

# Local secrets — never commit (use `dotnet user-secrets` for local dev)
appsettings.Production.json
appsettings.*.local.json

# Logs
logs/
*.log

# IIS publish output
publish/

# Frontend (Vite/React admin console)
frontend/node_modules/
frontend/dist/
frontend/.env.local
frontend/.env.*.local

# Frontend build output copied into the API's wwwroot before publishing
# (see the manual publish workflow — not built automatically by dotnet publish)
src/XDS_GHC_Verification/wwwroot/
